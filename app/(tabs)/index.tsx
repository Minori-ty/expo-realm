import PageHeader from '@/components/PageHeader'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'

const index = () => {
    return (
        <SafeAreaView>
            <PageHeader title="更新表" />
        </SafeAreaView>
    )
}

export default index
